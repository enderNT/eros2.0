services:
  agente:
    build: .
    env_file: .env
    ports:
      - "8000:8000"
    volumes:
      - agente-data:/data
    environment:
      DB_PATH: /data/agente.db
      CONTENT_DIR: /app/content
volumes:
  agente-data:
